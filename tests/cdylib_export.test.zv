// Shared C Dynamic Library (cdylib) Export Test (tests/cdylib_export.test.zv)

pub extern "C" {
    add a: num, b: num -> num {
        -> a + b
    }

    multiply a: num, b: num -> num {
        -> a * b
    }
}

pub extern "C" calculateTax price: num, rate: num -> num {
    -> price * rate
}

